# OMNIX — optional voice stack (dictation + read-aloud).
#
# Install on top of requirements.txt:
#   pip install -r requirements-voice.txt
#
# Without these the server still starts and every other feature works; the
# /api/tts and /api/stt endpoints return a 500 explaining what is missing and
# the composer's mic button is the only affected UI. That is deliberate — these
# four packages were the slowest and least reliable part of setting OMNIX up,
# and they gate a feature that is not why anyone buys it.

faster-whisper
piper-tts
sounddevice
webrtcvad-wheels
numpy
